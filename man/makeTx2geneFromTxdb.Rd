% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jambio-tx2gene.R
\name{makeTx2geneFromTxdb}
\alias{makeTx2geneFromTxdb}
\title{Make tx2gene data.frame from a TxDb object}
\usage{
makeTx2geneFromTxdb(txdb, ann_lib, verbose = FALSE, ...)
}
\arguments{
\item{txdb}{\code{TxDb} object, such as those provided by Bioconductor packages
like \code{TxDb.Mmusculus.UCSC.mm10.knownGene} or
\code{TxDb.Hsapiens.UCSC.hg38.knownGene}.}

\item{ann_lib}{\code{character} string naming an installed Bioconductor organism
annotation package (e.g. \code{"org.Mm.eg.db"} for mouse or
\code{"org.Hs.eg.db"} for human). Must match the organism used in \code{txdb}.
No validation of organism concordance is performed.}

\item{verbose}{\code{logical} whether to print verbose output during processing.}

\item{...}{additional arguments are ignored.}
}
\value{
\code{data.frame} with colnames \code{"transcript_id"}, \code{"gene_id"},
\code{"gene_name"}. \code{gene_id} contains the ENTREZID values from the \code{TxDb},
and \code{gene_name} contains the resolved gene SYMBOL.
}
\description{
Make tx2gene data.frame from a TxDb object and an org.*.eg.db annotation
package.
}
\details{
This function converts a Bioconductor \code{TxDb} annotation package (e.g.
\code{TxDb.Mmusculus.UCSC.mm10.knownGene}) into the three-column \code{data.frame}
expected by splicejam: \code{transcript_id}, \code{gene_id}, \code{gene_name}.

Gene IDs in UCSC-style \code{TxDb} objects are NCBI Entrez IDs (ENTREZID).
These are mapped to gene symbols (SYMBOL) using
\code{genejam::freshenGenes()} with the supplied \code{ann_lib}. When an ENTREZID
cannot be resolved to a SYMBOL, the original ENTREZID is retained as the
\code{gene_name} (controlled by \code{empty_rule="original"} in \code{freshenGenes()}).

\strong{Exception:} Some \code{TxDb} packages with suffix 'ensDb' use EnsEMBL
gene identifier, in the form 'ENSG00000001' or 'ENSMUSG00000001'.
In this case, \code{genejam::freshenGenes()} will be used to query by
the 'ENSEMBL2EG' annotation data, and will fallback to use the
EnsEMBL gene_id when not found.
}
\seealso{
\code{makeTx2geneFromGtf()} for the GTF-based equivalent,
\code{splicejamDataFromTxDb()} for the higher-level workflow that calls this
function.

Other GTF functions:
\code{\link[=describeGtfAttrNames]{describeGtfAttrNames()}},
\code{\link[=getGtfAttrs]{getGtfAttrs()}},
\code{\link[=makeTx2geneFromGtf]{makeTx2geneFromGtf()}},
\code{\link[=readGtf]{readGtf()}}
}
\concept{GTF functions}
