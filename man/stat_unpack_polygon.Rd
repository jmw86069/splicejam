% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/jambio-gg.R
\name{stat_unpack_polygon}
\alias{stat_unpack_polygon}
\alias{StatUnpackPolygon}
\title{Stat for unpacking list-column polygon coordinates}
\usage{
stat_unpack_polygon(
  mapping = NULL,
  data = NULL,
  geom = "polygon",
  position = "identity",
  na.rm = TRUE,
  show.legend = NA,
  inherit.aes = TRUE,
  ...
)
}
\arguments{
\item{na.rm}{logical. If \code{TRUE}, missing values are removed.
Default is \code{TRUE}.}

\item{...}{Additional arguments passed to the base stat.}
}
\description{
\code{stat_unpack_polygon()} expands compact list-column coordinates
stored as numeric vectors in \code{x} and \code{y} columns into long format
suitable for \code{geom_polygon()} and \code{geom_shape()}.
}
\details{
This stat is designed for memory-efficient storage of polygon
coordinates where each polygon occupies a single row, with \code{x}
and \code{y} columns containing numeric vectors of coordinates.
It unpacks these into the long format expected by ggplot2
polygon geoms during the statistical transformation layer.

The stat requires a grouping column (typically \code{id} or similar)
to distinguish between separate polygons when multiple polygons
are present in a single dataset. If no grouping is provided, each
row is treated as a separate polygon.
}
\section{Aesthetics}{

\code{stat_unpack_polygon()} requires the following aesthetics:
\itemize{
\item \code{x} — numeric vector (list-column) or numeric values
\item \code{y} — numeric vector (list-column) or numeric values
}

It can accept any aesthetics supported by \code{geom_polygon()} or
\code{geom_shape()}, such as \code{fill}, \code{colour}, \code{size}, \code{alpha}, \code{linetype}.
}

\examples{
# Create compact polygon data with list-column coordinates
polygon_data <- data.frame(
  id = c(1, 2),
  x = list(c(0, 1, 1, 0), c(2, 3, 3.5, 2)),
  y = list(c(0, 0, 1, 1), c(0, 0, 1.5, 1)),
  fill = c("red", "blue")
)

# Plot with stat_unpack_polygon
# ggplot(polygon_data, aes(x = x, y = y, fill = fill)) +
#   geom_polygon(stat = "unpack_polygon") +
#   coord_fixed()

}
\seealso{
Other jam ggplot2 functions:
\code{\link[=gene2gg]{gene2gg()}},
\code{\link[=geom_diagonal_wide_arc]{geom_diagonal_wide_arc()}},
\code{\link[=plotSashimi]{plotSashimi()}},
\code{\link{splicejam-extensions}},
\code{\link[=to_basic.GeomShape]{to_basic.GeomShape()}}
}
\concept{jam ggplot2 functions}
