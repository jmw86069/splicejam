% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/splicejam-shiny-data.R
\name{splicejamDataFromTxDb}
\alias{splicejamDataFromTxDb}
\title{Prepare splicejam data environment from a TxDb annotation package}
\usage{
splicejamDataFromTxDb(
  txdb,
  ann_lib = NULL,
  tx2geneDF = NULL,
  detectedTx = NULL,
  detectedGenes = NULL,
  filesDF = NULL,
  color_sub = NULL,
  verbose = FALSE,
  ...
)
}
\arguments{
\item{txdb}{\code{TxDb} object, typically loaded from a Bioconductor annotation
package such as \code{TxDb.Mmusculus.UCSC.mm10.knownGene}.}

\item{ann_lib}{\code{character} string naming an installed Bioconductor organism
annotation package (e.g. \code{"org.Mm.eg.db"}). Must correspond to the same
organism as \code{txdb}. No cross-validation is performed.
\itemize{
\item When NULL as default, it will try to generate a suitable annotation name
using the txdb metadata 'Organism', using the first letter of the first
two words in the organism name. This process works for most common species,
but will fail for things like E. coli K12.
}}

\item{tx2geneDF}{\code{data.frame}, default NULL.
\itemize{
\item When NULL, \code{ann_lib} is used to derive \code{tx2geneDF}.
\item When provided, it is used and \code{ann_lib} is ignored.
\itemize{
\item The 'gene_id' values must exactly match:
\code{names(GenomicFeatures::genes(txdb, single.strand.genes.only=FALSE))}.
\item The 'gene_name' column will be used to name gene data.
\item The 'transcript_id' values must exactly match values 'tx_name' from:
\code{GenomicFeatures::transcripts(txdb)}.
}
}}

\item{detectedTx}{\code{character} vector of \code{transcript_id} values representing
detected (expressed) transcripts. When \code{NULL} (default), all transcripts
in the \code{TxDb} are used. Values not found in \code{tx2geneDF$transcript_id}
are silently dropped.}

\item{detectedGenes}{\code{character} vector of \code{gene_name} (SYMBOL) values
representing genes of interest. When \code{NULL} (default), genes are derived
from \code{detectedTx} via \code{tx2geneDF}. Values not found in
\code{tx2geneDF$gene_name} are silently dropped.}

\item{filesDF}{optional \code{data.frame} with at minimum colnames \code{"sample_id"},
\code{"url"}, \code{"type"}. Stored in the returned environment unchanged.}

\item{color_sub}{optional named \code{character} vector of R colors, whose names
should match \code{filesDF$sample_id}. When \code{filesDF} is provided but
\code{color_sub} is \code{NULL}, colors are derived automatically using
\code{colorjam::group2colors()}.}

\item{verbose}{\code{logical} whether to print verbose output.}

\item{...}{additional arguments are ignored.}
}
\value{
\code{environment} containing:
\itemize{
\item \code{flatExonsByGene} — \code{GRangesList} with non-overlapping exons per gene,
named by \code{gene_name}.
\item \code{flatExonsByTx} — \code{GRangesList} with non-overlapping exons per
transcript, named by \code{transcript_id}.
\item \code{tx2geneDF} — \code{data.frame} with colnames \code{"transcript_id"}, \code{"gene_id"},
\code{"gene_name"}, subset to detected transcripts and genes.
\item \code{detectedTx} — \code{character} vector of retained transcript IDs.
\item \code{detectedGenes} — \code{character} vector of retained gene names.
\item \code{filesDF} — included when provided (otherwise absent).
\item \code{color_sub} — included when provided or derived from \code{filesDF}.
}
}
\description{
Prepare splicejam data environment from a TxDb annotation package
}
\details{
This function is an alternative to \code{sashimiDataConstants()} for users who
prefer to supply a Bioconductor \code{TxDb} annotation package such as
\code{'TxDb.Mmusculus.UCSC.mm10.knownGene'}, rather than a GTF file.

It calls \code{makeTx2geneFromTxdb()} to build the transcript-to-gene
\code{data.frame} 'tx2geneDF',
then derives \code{exonsByTx}, \code{cdsByTx}, \code{flatExonsByTx}, and \code{flatExonsByGene}
from the \code{TxDb} using the same \code{flattenExonsBy()} machinery as the
GTF-based workflow.
}
\section{Subsetting logic}{

When \code{detectedTx} and/or \code{detectedGenes} are provided, they are used to
subset the full annotation:
\itemize{
\item \code{detectedTx} is intersected with \code{tx2geneDF$transcript_id}.
Providing \code{detectedTx} also limits the available genes via \code{tx2geneDF}.
\item \code{detectedGenes} is intersected with \code{tx2geneDF$gene_name}.
Providing \code{detectedGenes} also limits the available transcripts via
\code{tx2geneDF}.
\item Both filters are applied simultaneously; the resulting \code{tx2geneDF} is the
source of truth from which \code{detectedTx} and \code{detectedGenes} are
re-derived.
\item When neither is provided, all transcripts and genes in the \code{TxDb} are
used.
}
}

\examples{
\dontrun{
# Prepare TxDb for mouse mm10 genome
system.time({
   sjenv_txdb <- splicejamDataFromTxDb(
      txdb=TxDb.Mmusculus.UCSC.mm10.knownGene::TxDb.Mmusculus.UCSC.mm10.knownGene,
      detectedGenes=unique(sjenvtest$tx2geneDF$gene_name),
      filesDF=sjenvtest$filesDF,
      ann_lib="org.Mm.eg.db")
})
# 8 to 12 seconds to prep the environment

# optionally display progress bar
progressr::handlers(global=TRUE)
system.time({
   splicejamFigure(sjenv_txdb,
      gene="Gria1",
      geneAxisSize=8,
      use_memoise=TRUE)
})

# Use subset of transcript_id which happen
# to be compatible with sjenvtest on mm10.
# Note the flat gene exons still used all
# transcripts. Rebuild from TxDb to resolve.
use_detectedTx <- subset(sjenvtest$tx2geneDF,
   gene_name \%in\% 'Gria1')$transcript_id
system.time({
   splicejamFigure(sjenv_txdb,
      gene="Gria1",
      geneAxisSize=8,
      minJunctionScore=100,
      detectedTx=use_detectedTx,
      use_exon_range=c("exon17", "exon20"),
      use_memoise=TRUE)
})
}
}
\seealso{
\code{sashimiDataConstants()} for the GTF-based equivalent,
\code{makeTx2geneFromTxdb()} for the helper that builds \code{tx2geneDF}.

Other Splicejam core functions:
\code{\link[=launchSashimiApp]{launchSashimiApp()}},
\code{\link[=sashimiDataConstants]{sashimiDataConstants()}},
\code{\link[=splicejamFigure]{splicejamFigure()}}
}
\concept{Splicejam core functions}
